# ml-cpu-pipeline-prediction
An ML-based CPU pipeline analysis and prediction system that simulates instruction sequences, analyzes hazards and stalls, generates a custom dataset, and predicts pipeline performance metrics such as CPI and total execution cycles.
